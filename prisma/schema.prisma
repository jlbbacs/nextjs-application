// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
  // Keep your output path if you changed it, otherwise:
  output   = "../generated/client"
}

datasource db {
  provider = "sqlite"
}

model Post {
  id      Int    @id @default(autoincrement())
  title   String
  content String
}